//#region lib/types/index.js
/** Host loader entry for the browser-only auto-collapse-sidebar plugin. */
/** Provides no host-side behavior; the preference is process-local. */
function apply() {}
//#endregion
export { apply };
